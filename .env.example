# Copier ce fichier vers .env et renseigner les valeurs
DB_HOST=localhost
DB_PORT=5432
DB_NAME=epic_events
DB_USER=epic_events_user
DB_PASSWORD=changez_moi

# JWT (authentification persistante)
JWT_SECRET=changez_moi_par_une_chaine_longue_et_aleatoire
JWT_ALGORITHM=HS256
JWT_EXPIRE_HOURS=8

# Sentry (laisser vide pour désactiver la journalisation distante)
SENTRY_DSN=
SENTRY_ENVIRONMENT=development
